import React, { Component } from 'react';

class FaviconControl extends Component {
  startRotatingFavicon = () => {
    const link = document.querySelector("link[rel*='icon']");
    if (link) {
      link.classList.add('rotate-favicon');
    }
  }

  stopRotatingFavicon = () => {
    const link = document.querySelector("link[rel*='icon']");
    if (link) {
      link.classList.remove('rotate-favicon');
    }
  }

  render() {
    return (
      <div>
        <button onClick={this.startRotatingFavicon}>Start Rotating Favicon</button>
        <button onClick={this.stopRotatingFavicon}>Stop Rotating Favicon</button>
      </div>
    );
  }
}

export default FaviconControl;

.....

import React from 'react';
import FaviconControl from './FaviconControl';

function App() {
  return (
    <div>
      {/* Other components or content */}
      <FaviconControl />
    </div>
  );
}

export default App;
.....
<!-- Your JSP page content -->
<div id="root"></div>

<!-- Include the React app script (usually generated by a build tool like Webpack) -->
<script src="path-to-your-react-app.js"></script>

<script>
  // Render your React app into the 'root' element
  ReactDOM.render(
    React.createElement(App),
    document.getElementById('root')
  );
</script>
