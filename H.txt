import React, { useState } from "react";

const Help = () => {
  const [ticketText, setTicketText] = useState("");
  const [ticketSubmitted, setTicketSubmitted] = useState(false);

  const submitTicket = () => {
    // Your logic to submit the ticket (e.g., send to server, update database, etc.)
    // This is a placeholder, you would replace it with actual implementation
    console.log("Ticket submitted:", ticketText);
    setTicketSubmitted(true);
  };

  return (
    <div className="help-container">
      <h1 className="help-heading">PharmaEasy Help Center</h1>
      <p className="help-intro">
        Welcome to PharmaEasy's Help Center. If you have any questions or need assistance, please refer to the information below or contact our support team.
      </p>

      {/* Existing sections... */}

      <div className="customer-support-section">
        <h2 className="customer-support-heading">Customer Support</h2>
        <p className="customer-support-info">
          If you need immediate assistance, you can raise a support ticket.
        </p>

        {ticketSubmitted ? (
          <p className="ticket-submitted-message">
            Your ticket has been submitted. Our support team will get back to you shortly.
          </p>
        ) : (
          <div className="raise-ticket-form">
            <textarea
              className="ticket-textarea"
              placeholder="Describe your issue or question..."
              value={ticketText}
              onChange={(e) => setTicketText(e.target.value)}
            />
            <button className="submit-ticket-button" onClick={submitTicket}>
              Raise Ticket
            </button>
          </div>
        )}
      </div>

      {/* Additional help content can be added as needed */}
    </div>
  );
};

export default Help;
////

/* Add these styles to your CSS file or component */

.customer-support-section {
  margin-top: 20px;
}

.customer-support-heading {
  color: #008080; /* Teal color */
}

.customer-support-info {
  color: #555; /* Medium gray color */
}

.ticket-submitted-message {
  color: #008000; /* Green color */
}

.raise-ticket-form {
  margin-top: 10px;
}

.ticket-textarea {
  width: 100%;
  height: 100px;
  margin-bottom: 10px;
}

.submit-ticket-button {
  background-color: #008080; /* Teal color */
  color: #fff; /* White color */
  padding: 10px 15px;
  border: none;
  cursor: pointer;
  border-radius: 5px;
}
